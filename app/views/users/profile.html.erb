<div class='container text-center'>
    <h1>User Profile</h1>
    <div class='row'> 

        <div class='col-sm-6'>
            <h3>All your events: </h3>
            <% @events_created_by_user.each do |event|%>
                <%= render partial: 'events/event' , locals: {event: event} %>
            <% end%>
        </div>

        <div class='col-sm-6'>
            <h3>Attending events: </h3>
            <% @attending_events.each do |event|%>
                <%= render partial: 'events/event' , locals: {event: event} %>
            <% end%>
            
        </div>

    </div>
</div>